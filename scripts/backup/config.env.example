# Database Backup Configuration
# Copy this file to config.env and fill in your values

# Database Connection
DB_HOST=localhost
DB_PORT=5432
DB_NAME=rappit
DB_USER=rappit_user
# DB_PASSWORD should be set via PGPASSWORD environment variable or .pgpass file

# Backup Settings
BACKUP_DIR=/var/backups/rappit
RETENTION_DAYS=30

# Environment (development, staging, production)
ENVIRONMENT=development

# Logging
LOG_DIR=/var/log/rappit-backup

# Alert Settings (for future AWS/PagerDuty integration)
# PAGERDUTY_SERVICE_KEY=your-pagerduty-key
# S3_BUCKET=rappit-backups-us-east-1-production
# AWS_REGION=us-east-1
